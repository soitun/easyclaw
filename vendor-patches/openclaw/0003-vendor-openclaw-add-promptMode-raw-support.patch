From b54f4c60e98b69043e9fca2cc6a3dd0c9da210b1 Mon Sep 17 00:00:00 2001
From: RivonClaw CI <ci@rivonclaw.com>
Date: Fri, 10 Apr 2026 18:17:24 -0700
Subject: [PATCH] vendor(openclaw): add promptMode "raw" for custom persona
 agents

Why:
- EasyClaw's customer-service agent must not reveal it is an AI, but
  OpenClaw injects identity ("You are a personal assistant running
  inside OpenClaw"), runtime info (model=..., default_model=...),
  safety guidelines (Anthropic constitution reference), heartbeat/
  silent-reply tokens, and documentation links into every system prompt.
  These sections leak AI identity even when extraSystemPrompt explicitly
  instructs otherwise, because they appear before the custom prompt.
- promptMode "none" still injects the identity line.
- promptMode "raw" returns *only* extraSystemPrompt with zero hardcoded
  content, giving the caller full control over the agent persona.
- Threads promptMode through the agent RPC schema, command opts, run
  params, attempt, and compact paths so callers can pass it via RPC.

Removal:
- Drop when upstream OpenClaw adds a native way to fully suppress all
  default system prompt sections (e.g. a "raw" or "custom" promptMode,
  or a per-section suppression API).

Tests:
- apps/desktop/src/cs-bridge/vendor-prompt-raw.sentinel.test.ts
---
 src/agents/cli-runner/types.ts                 | 3 ++-
 src/agents/command/attempt-execution.ts        | 1 +
 src/agents/pi-embedded-runner/compact.ts       | 5 +++--
 src/agents/pi-embedded-runner/compact.types.ts | 2 ++
 src/agents/system-prompt.ts                    | 7 +++++++
 src/agents/system-prompt.types.ts              | 2 +-
 src/gateway/protocol/schema/agent.ts           | 7 ++++++-
 src/gateway/server-methods/agent.ts            | 2 +-
 8 files changed, 23 insertions(+), 6 deletions(-)

diff --git a/src/agents/cli-runner/types.ts b/src/agents/cli-runner/types.ts
index 73c1ea4359..e87f41cff8 100644
--- a/src/agents/cli-runner/types.ts
+++ b/src/agents/cli-runner/types.ts
@@ -11,7 +11,7 @@ import type { InputProvenance } from "../../sessions/input-provenance.js";
 import type { ResolvedCliBackend } from "../cli-backends.js";
 import type { EmbeddedRunTrigger } from "../pi-embedded-runner/run/params.js";
 import type { SkillSnapshot } from "../skills.js";
-import type { SilentReplyPromptMode } from "../system-prompt.types.js";
+import type { PromptMode, SilentReplyPromptMode } from "../system-prompt.types.js";
 
 export type RunCliAgentParams = {
   sessionId: string;
@@ -32,6 +32,7 @@ export type RunCliAgentParams = {
   lane?: string;
   jobId?: string;
   extraSystemPrompt?: string;
+  promptMode?: PromptMode;
   sourceReplyDeliveryMode?: SourceReplyDeliveryMode;
   silentReplyPromptMode?: SilentReplyPromptMode;
   /** Static portion of extraSystemPrompt (excluding per-message inbound metadata) for session reuse hashing. */
diff --git a/src/agents/command/attempt-execution.ts b/src/agents/command/attempt-execution.ts
index 1ef26a11f0..aea0a510eb 100644
--- a/src/agents/command/attempt-execution.ts
+++ b/src/agents/command/attempt-execution.ts
@@ -481,6 +481,7 @@ export function runAgentAttempt(params: {
         timeoutMs: params.timeoutMs,
         runId: params.runId,
         extraSystemPrompt: params.opts.extraSystemPrompt,
+        promptMode: params.opts.promptMode,
         inputProvenance: params.opts.inputProvenance,
         cliSessionId: nextCliSessionId,
         cliSessionBinding:
diff --git a/src/agents/pi-embedded-runner/compact.ts b/src/agents/pi-embedded-runner/compact.ts
index 5deb46d5fa..814358f4f4 100644
--- a/src/agents/pi-embedded-runner/compact.ts
+++ b/src/agents/pi-embedded-runner/compact.ts
@@ -830,9 +830,10 @@ async function compactEmbeddedPiSessionDirectOnce(
     const userTimeFormat = resolveUserTimeFormat(params.config?.agents?.defaults?.timeFormat);
     const userTime = formatUserTime(new Date(), userTimezone, userTimeFormat);
     const promptMode =
-      isSubagentSessionKey(params.sessionKey) || isCronSessionKey(params.sessionKey)
+      params.promptMode ??
+      (isSubagentSessionKey(params.sessionKey) || isCronSessionKey(params.sessionKey)
         ? "minimal"
-        : "full";
+        : "full");
     const openClawReferences = await resolveOpenClawReferencePaths({
       workspaceDir: effectiveWorkspace,
       argv1: process.argv[1],
diff --git a/src/agents/pi-embedded-runner/compact.types.ts b/src/agents/pi-embedded-runner/compact.types.ts
index bb67c329df..270285cafe 100644
--- a/src/agents/pi-embedded-runner/compact.types.ts
+++ b/src/agents/pi-embedded-runner/compact.types.ts
@@ -74,6 +74,8 @@ export type CompactEmbeddedPiSessionParams = {
   abortSignal?: AbortSignal;
   /** Allow runtime plugins for this compaction to late-bind the gateway subagent. */
   allowGatewaySubagentBinding?: boolean;
+  /** Override the automatic promptMode resolution (e.g. "raw" for custom persona agents). */
+  promptMode?: string;
 };
 
 export type CompactionMessageMetrics = {
diff --git a/src/agents/system-prompt.ts b/src/agents/system-prompt.ts
index 0a628d123a..cf09705e3b 100644
--- a/src/agents/system-prompt.ts
+++ b/src/agents/system-prompt.ts
@@ -750,6 +750,13 @@ export function buildAgentSystemPrompt(params: {
   });
   const workspaceNotes = (params.workspaceNotes ?? []).map((note) => note.trim()).filter(Boolean);
 
+  // For "raw" mode, return only the caller-supplied extraSystemPrompt with no
+  // hardcoded identity, safety, runtime, or tooling sections. This lets the
+  // caller fully control the agent persona.
+  if (promptMode === "raw") {
+    return params.extraSystemPrompt ?? "";
+  }
+
   // For "none" mode, return just the basic identity line
   if (promptMode === "none") {
     return "You are a personal assistant running inside OpenClaw.";
diff --git a/src/agents/system-prompt.types.ts b/src/agents/system-prompt.types.ts
index 2144733cdf..a4ebae92c4 100644
--- a/src/agents/system-prompt.types.ts
+++ b/src/agents/system-prompt.types.ts
@@ -1,2 +1,2 @@
-export type PromptMode = "full" | "minimal" | "none";
+export type PromptMode = "full" | "minimal" | "none" | "raw";
 export type SilentReplyPromptMode = "generic" | "none";
diff --git a/src/gateway/protocol/schema/agent.ts b/src/gateway/protocol/schema/agent.ts
index 6d45130187..a2c25ab581 100644
--- a/src/gateway/protocol/schema/agent.ts
+++ b/src/gateway/protocol/schema/agent.ts
@@ -159,7 +159,12 @@ export const AgentParamsSchema = Type.Object(
     cleanupBundleMcpOnRunEnd: Type.Optional(Type.Boolean()),
     modelRun: Type.Optional(Type.Boolean()),
     promptMode: Type.Optional(
-      Type.Union([Type.Literal("full"), Type.Literal("minimal"), Type.Literal("none")]),
+      Type.Union([
+        Type.Literal("full"),
+        Type.Literal("minimal"),
+        Type.Literal("none"),
+        Type.Literal("raw"),
+      ]),
     ),
     extraSystemPrompt: Type.Optional(Type.String()),
     bootstrapContextMode: Type.Optional(
diff --git a/src/gateway/server-methods/agent.ts b/src/gateway/server-methods/agent.ts
index 911924d919..b4f5dead39 100644
--- a/src/gateway/server-methods/agent.ts
+++ b/src/gateway/server-methods/agent.ts
@@ -530,7 +530,7 @@ export const agentHandlers: GatewayRequestHandlers = {
       lane?: string;
       extraSystemPrompt?: string;
       modelRun?: boolean;
-      promptMode?: "full" | "minimal" | "none";
+      promptMode?: "full" | "minimal" | "none" | "raw";
       bootstrapContextMode?: "full" | "lightweight";
       bootstrapContextRunKind?: "default" | "heartbeat" | "cron";
       acpTurnSource?: "manual_spawn";
-- 
2.36.1.windows.1

