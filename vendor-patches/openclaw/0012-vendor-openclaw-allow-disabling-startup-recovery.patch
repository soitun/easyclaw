From 8238e6589d6d5a53d7c93cd10b36df5abf066d02 Mon Sep 17 00:00:00 2001
From: RivonClaw Vendor Replay <vendor-replay@rivonclaw.invalid>
Date: Wed, 6 May 2026 07:23:04 -0700
Subject: [PATCH] vendor(openclaw): allow disabling startup recovery

Why:
- RivonClaw receives customer-service retries from backend/Airflow rather than relying on local gateway replay.
- OpenClaw startup delivery recovery and interrupted-session recovery can overwhelm the single desktop gateway process when many customer-service deliveries or aborted WeChat sessions are queued.

Removal:
- Drop when upstream OpenClaw exposes first-class config for startup recovery policies or makes startup recovery budgeted enough for RivonClaw customer-service workloads.

Tests:
- vendor/openclaw/src/gateway/server-runtime-services.test.ts
- vendor/openclaw/src/gateway/server-startup-post-attach.test.ts
- apps/desktop/src/infra/proxy/proxy-manager.test.ts
---
 src/gateway/server-runtime-services.test.ts   | 27 ++++++++++++++
 src/gateway/server-runtime-services.ts        | 20 +++++++++-
 .../server-startup-post-attach.test.ts        | 37 +++++++++++++++++++
 src/gateway/server-startup-post-attach.ts     | 11 +++++-
 src/gateway/server.impl.ts                    |  1 +
 5 files changed, 94 insertions(+), 2 deletions(-)

diff --git a/src/gateway/server-runtime-services.test.ts b/src/gateway/server-runtime-services.test.ts
index 72dca20d54..0cf8ca390a 100644
--- a/src/gateway/server-runtime-services.test.ts
+++ b/src/gateway/server-runtime-services.test.ts
@@ -26,6 +26,8 @@ vi.mock("../infra/heartbeat-runner.js", () => ({
 
 vi.mock("../infra/env.js", () => ({
   isVitestRuntimeEnv: hoisted.isVitestRuntimeEnv,
+  isTruthyEnvValue: (value: unknown) =>
+    typeof value === "string" && ["1", "true", "yes", "on"].includes(value.trim().toLowerCase()),
 }));
 
 vi.mock("../infra/outbound/deliver.js", () => ({
@@ -221,6 +223,31 @@ describe("server-runtime-services", () => {
     expect(hoisted.recoverPendingDeliveries).toHaveBeenCalledTimes(1);
   });
 
+  it("can disable startup delivery and session restart recovery by environment", async () => {
+    vi.useFakeTimers();
+    const cron = { start: vi.fn(async () => undefined) };
+
+    activateGatewayScheduledServices({
+      minimalTestGateway: false,
+      cfgAtStart: {} as never,
+      deps: {} as never,
+      sessionDeliveryRecoveryMaxEnqueuedAt: 123,
+      cron,
+      logCron: { error: vi.fn() },
+      log: createLog(),
+      env: {
+        OPENCLAW_DISABLE_OUTBOUND_DELIVERY_RECOVERY: "1",
+        OPENCLAW_DISABLE_SESSION_RESTART_RECOVERY: "1",
+      } as never,
+    });
+
+    await vi.advanceTimersByTimeAsync(1_250);
+    await vi.dynamicImportSettled();
+
+    expect(hoisted.recoverPendingDeliveries).not.toHaveBeenCalled();
+    expect(hoisted.recoverPendingRestartContinuationDeliveries).not.toHaveBeenCalled();
+  });
+
   it("starts cron and records memory when post-ready maintenance fails", async () => {
     const cron = { start: vi.fn(async () => undefined) };
     const log = createLog();
diff --git a/src/gateway/server-runtime-services.ts b/src/gateway/server-runtime-services.ts
index d5d0847e28..89aa56e475 100644
--- a/src/gateway/server-runtime-services.ts
+++ b/src/gateway/server-runtime-services.ts
@@ -1,5 +1,5 @@
 import type { OpenClawConfig } from "../config/types.openclaw.js";
-import { isVitestRuntimeEnv } from "../infra/env.js";
+import { isTruthyEnvValue, isVitestRuntimeEnv } from "../infra/env.js";
 import { startHeartbeatRunner, type HeartbeatRunner } from "../infra/heartbeat-runner.js";
 import type { PluginMetadataRegistryView } from "../plugins/plugin-metadata-snapshot.types.js";
 import type { ChannelHealthMonitor } from "./channel-health-monitor.js";
@@ -7,6 +7,9 @@ import { startChannelHealthMonitor } from "./channel-health-monitor.js";
 import { isGatewayModelPricingEnabled } from "./model-pricing-config.js";
 import type { startGatewayMaintenanceTimers } from "./server-maintenance.js";
 
+const DISABLE_OUTBOUND_DELIVERY_RECOVERY_ENV = "OPENCLAW_DISABLE_OUTBOUND_DELIVERY_RECOVERY";
+const DISABLE_SESSION_RESTART_RECOVERY_ENV = "OPENCLAW_DISABLE_SESSION_RESTART_RECOVERY";
+
 type GatewayRuntimeServiceLogger = {
   child: (name: string) => {
     info: (message: string) => void;
@@ -156,7 +159,13 @@ export function scheduleGatewayPostReadyMaintenance(params: {
 function recoverPendingOutboundDeliveries(params: {
   cfg: OpenClawConfig;
   log: GatewayRuntimeServiceLogger;
+  env?: NodeJS.ProcessEnv;
 }): void {
+  if (isTruthyEnvValue(params.env?.[DISABLE_OUTBOUND_DELIVERY_RECOVERY_ENV])) {
+    params.log.child("delivery-recovery").info("disabled by environment");
+    return;
+  }
+
   void (async () => {
     const { recoverPendingDeliveries } = await import("../infra/outbound/delivery-queue.js");
     const { deliverOutboundPayloads } = await import("../infra/outbound/deliver.js");
@@ -173,7 +182,13 @@ function recoverPendingSessionDeliveries(params: {
   deps: import("../cli/deps.types.js").CliDeps;
   log: GatewayRuntimeServiceLogger;
   maxEnqueuedAt: number;
+  env?: NodeJS.ProcessEnv;
 }): void {
+  if (isTruthyEnvValue(params.env?.[DISABLE_SESSION_RESTART_RECOVERY_ENV])) {
+    params.log.child("session-delivery-recovery").info("disabled by environment");
+    return;
+  }
+
   const timer = setTimeout(() => {
     void (async () => {
       const { recoverPendingRestartContinuationDeliveries } =
@@ -252,6 +267,7 @@ export function activateGatewayScheduledServices(params: {
   logCron: { error: (message: string) => void };
   log: GatewayRuntimeServiceLogger;
   pluginLookUpTable?: PluginMetadataRegistryView;
+  env?: NodeJS.ProcessEnv;
 }): { heartbeatRunner: HeartbeatRunner; stopModelPricingRefresh: () => void } {
   if (params.minimalTestGateway) {
     return { heartbeatRunner: createNoopHeartbeatRunner(), stopModelPricingRefresh: () => {} };
@@ -266,11 +282,13 @@ export function activateGatewayScheduledServices(params: {
   recoverPendingOutboundDeliveries({
     cfg: params.cfgAtStart,
     log: params.log,
+    env: params.env ?? process.env,
   });
   recoverPendingSessionDeliveries({
     deps: params.deps,
     log: params.log,
     maxEnqueuedAt: params.sessionDeliveryRecoveryMaxEnqueuedAt,
+    env: params.env ?? process.env,
   });
   const stopModelPricingRefresh = !isVitestRuntimeEnv()
     ? startGatewayModelPricingRefreshOnDemand({
diff --git a/src/gateway/server-startup-post-attach.test.ts b/src/gateway/server-startup-post-attach.test.ts
index e7efc7b135..250380ed5c 100644
--- a/src/gateway/server-startup-post-attach.test.ts
+++ b/src/gateway/server-startup-post-attach.test.ts
@@ -27,6 +27,8 @@ const hoisted = vi.hoisted(() => {
   const refreshLatestUpdateRestartSentinel = vi.fn<
     typeof import("./server-restart-sentinel.js").refreshLatestUpdateRestartSentinel
   >(async () => null);
+  const markRestartAbortedMainSessionsFromLocks = vi.fn(async () => undefined);
+  const scheduleRestartAbortedMainSessionRecovery = vi.fn();
   const getAcpRuntimeBackend = vi.fn<(id?: string) => unknown>(() => null);
   const reconcilePendingSessionIdentities = vi.fn(async () => ({
     checked: 0,
@@ -60,6 +62,8 @@ const hoisted = vi.hoisted(() => {
     shouldWakeFromRestartSentinel,
     scheduleRestartSentinelWake,
     refreshLatestUpdateRestartSentinel,
+    markRestartAbortedMainSessionsFromLocks,
+    scheduleRestartAbortedMainSessionRecovery,
     getAcpRuntimeBackend,
     reconcilePendingSessionIdentities,
     resolveAgentModelPrimaryValue,
@@ -80,6 +84,11 @@ vi.mock("../agents/session-write-lock.js", () => ({
   cleanStaleLockFiles: vi.fn(async () => undefined),
 }));
 
+vi.mock("../agents/main-session-restart-recovery.js", () => ({
+  markRestartAbortedMainSessionsFromLocks: hoisted.markRestartAbortedMainSessionsFromLocks,
+  scheduleRestartAbortedMainSessionRecovery: hoisted.scheduleRestartAbortedMainSessionRecovery,
+}));
+
 vi.mock("../agents/subagent-registry.js", () => ({
   scheduleSubagentOrphanRecovery: hoisted.scheduleSubagentOrphanRecovery,
 }));
@@ -212,6 +221,8 @@ describe("startGatewayPostAttachRuntime", () => {
     hoisted.scheduleSubagentOrphanRecovery.mockClear();
     hoisted.shouldWakeFromRestartSentinel.mockReturnValue(false);
     hoisted.scheduleRestartSentinelWake.mockClear();
+    hoisted.markRestartAbortedMainSessionsFromLocks.mockClear();
+    hoisted.scheduleRestartAbortedMainSessionRecovery.mockClear();
     hoisted.getAcpRuntimeBackend.mockReset();
     hoisted.getAcpRuntimeBackend.mockReturnValue(null);
     hoisted.reconcilePendingSessionIdentities.mockClear();
@@ -631,6 +642,32 @@ describe("startGatewayPostAttachRuntime", () => {
     );
   });
 
+  it("skips main session restart recovery when disabled by environment", async () => {
+    await withEnvAsync({ OPENCLAW_DISABLE_SESSION_RESTART_RECOVERY: "1" }, async () => {
+      await startGatewaySidecars({
+        cfg: {
+          hooks: { internal: { enabled: false } },
+        } as never,
+        pluginRegistry: createPostAttachParams().pluginRegistry,
+        defaultWorkspaceDir: "/tmp/openclaw-workspace",
+        deps: {} as never,
+        startChannels: vi.fn(async () => undefined),
+        log: { warn: vi.fn() },
+        logHooks: {
+          info: vi.fn(),
+          warn: vi.fn(),
+          error: vi.fn(),
+        },
+        logChannels: {
+          info: vi.fn(),
+          error: vi.fn(),
+        },
+      });
+    });
+
+    expect(hoisted.scheduleRestartAbortedMainSessionRecovery).not.toHaveBeenCalled();
+  });
+
   it("keeps startup-gated methods unavailable while sidecars are still resuming", async () => {
     let resumeSidecars!: () => void;
     const sidecarsReady = new Promise<{ pluginServices: null }>((resolve) => {
diff --git a/src/gateway/server-startup-post-attach.ts b/src/gateway/server-startup-post-attach.ts
index ca421e2871..92902db59a 100644
--- a/src/gateway/server-startup-post-attach.ts
+++ b/src/gateway/server-startup-post-attach.ts
@@ -29,6 +29,7 @@ const ACP_BACKEND_READY_POLL_MS = 50;
 const PRIMARY_MODEL_PREWARM_TIMEOUT_MS = 5_000;
 const STARTUP_PROVIDER_DISCOVERY_TIMEOUT_MS = 5_000;
 const SKIP_STARTUP_MODEL_PREWARM_ENV = "OPENCLAW_SKIP_STARTUP_MODEL_PREWARM";
+const DISABLE_SESSION_RESTART_RECOVERY_ENV = "OPENCLAW_DISABLE_SESSION_RESTART_RECOVERY";
 const QMD_STARTUP_IDLE_DELAY_MS = 120_000;
 const RESTART_SENTINEL_FILENAME = "restart-sentinel.json";
 
@@ -62,6 +63,10 @@ function shouldSkipStartupModelPrewarm(env: NodeJS.ProcessEnv = process.env): bo
   return raw === "1" || raw === "true" || raw === "yes" || raw === "on";
 }
 
+function shouldDisableSessionRestartRecovery(env: NodeJS.ProcessEnv = process.env): boolean {
+  return isTruthyEnvValue(env[DISABLE_SESSION_RESTART_RECOVERY_ENV]);
+}
+
 function resolveGatewayMemoryStartupPolicy(cfg: OpenClawConfig): GatewayMemoryStartupPolicy {
   if (cfg.memory?.backend !== "qmd") {
     return { mode: "off" };
@@ -570,7 +575,7 @@ export async function startGatewaySidecars(params: {
             removeStale: true,
             log: { warn: (message) => params.log.warn(message) },
           });
-          if (result.cleaned.length > 0) {
+          if (result.cleaned.length > 0 && !shouldDisableSessionRestartRecovery()) {
             const { markRestartAbortedMainSessionsFromLocks } =
               await import("../agents/main-session-restart-recovery.js");
             await markRestartAbortedMainSessionsFromLocks({
@@ -623,6 +628,9 @@ export async function startGatewaySidecars(params: {
     name: "sidecars.main-session-recovery",
     log: params.log,
     run: async () => {
+      if (shouldDisableSessionRestartRecovery()) {
+        return;
+      }
       const { scheduleRestartAbortedMainSessionRecovery } =
         await import("../agents/main-session-restart-recovery.js");
       scheduleRestartAbortedMainSessionRecovery();
@@ -874,5 +882,6 @@ export const __testing = {
   refreshLatestUpdateRestartSentinelIfPresent,
   resolveGatewayMemoryStartupPolicy,
   schedulePrimaryModelPrewarm,
+  shouldDisableSessionRestartRecovery,
   shouldSkipStartupModelPrewarm,
 };
diff --git a/src/gateway/server.impl.ts b/src/gateway/server.impl.ts
index aa13bd722a..43413b949b 100644
--- a/src/gateway/server.impl.ts
+++ b/src/gateway/server.impl.ts
@@ -1391,6 +1391,7 @@ export async function startGatewayServer(
         logCron,
         log,
         pluginLookUpTable,
+        env: process.env,
       });
       scheduledServicesActivated = true;
       runtimeState.heartbeatRunner = activated.heartbeatRunner;
-- 
2.50.1 (Apple Git-155)

